# Resumo da quantidade de cada landmark visto pelo vision_pkg dentro
# de uma janela de frames (padrão: 60 frames). Publicado pelo nó
# "landmark_summary_node" (pacote goleiro_decision) no tópico "robot_behavior".

int32 l_count
int32 t_count
int32 x_count
int32 goalpost_count
